{
  "name": "offgrid-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create offgrid-db",
    "db:migrate": "wrangler d1 execute offgrid-db --remote --file=migrations/001_init.sql",
    "db:migrate:local": "wrangler d1 execute offgrid-db --local --file=migrations/001_init.sql",
    "db:migrate:all": "wrangler d1 execute offgrid-db --remote -y --file=migrations/001_init.sql && wrangler d1 execute offgrid-db --remote -y --file=migrations/002_users_multitenant.sql && wrangler d1 execute offgrid-db --remote -y --file=migrations/003_tracklist.sql && wrangler d1 execute offgrid-db --remote -y --file=migrations/004_content_ownership.sql && wrangler d1 execute offgrid-db --remote -y --file=migrations/005_login_attempts.sql && wrangler d1 execute offgrid-db --remote -y --file=migrations/006_track_url.sql && wrangler d1 execute offgrid-db --remote -y --file=migrations/007_play_tracking.sql && wrangler d1 execute offgrid-db --remote -y --file=migrations/008_publish_state.sql",
    "db:migrate:all:local": "wrangler d1 execute offgrid-db --local -y --file=migrations/001_init.sql && wrangler d1 execute offgrid-db --local -y --file=migrations/002_users_multitenant.sql && wrangler d1 execute offgrid-db --local -y --file=migrations/003_tracklist.sql && wrangler d1 execute offgrid-db --local -y --file=migrations/004_content_ownership.sql && wrangler d1 execute offgrid-db --local -y --file=migrations/005_login_attempts.sql && wrangler d1 execute offgrid-db --local -y --file=migrations/006_track_url.sql && wrangler d1 execute offgrid-db --local -y --file=migrations/007_play_tracking.sql && wrangler d1 execute offgrid-db --local -y --file=migrations/008_publish_state.sql"
  },
  "devDependencies": {
    "wrangler": "^4.84.1"
  }
}
